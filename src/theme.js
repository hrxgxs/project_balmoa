// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  // 테마 설정
  palette: {
    primary: {
      main: '#1976D2',
    },
    secondary: {
      main: '#F50057',
    },
  },
});

export default theme;
